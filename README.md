Simple server base on Express.js that run apps in local server environment.

How to use:

- build your project;
- insert all files to build directory;
- run in terminal 'npm run server';
- open http://localhost:5000/ in your browser.
